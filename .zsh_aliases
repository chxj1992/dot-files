
# For Short 

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

alias cls='clear'

# 易错命令
alias LS='ls'
alias sl='ls'
alias CD='cd'

alias vi='vim'
alias sudo='sudo '

# Plugins
alias fuck='eval $(thefuck $(fc -ln -1))'
alias poli='http_proxy=http://localhost:8123'


#docker
alias docker-stop-all 'docker ps -a | awk '{print $1}' | xargs docker stop'
alias docker-rm-all 'docker ps -a | awk '{print $1}' | xargs docker rm'


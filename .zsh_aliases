
# For Short 

# some more ls aliases
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'

alias cls='clear'

# 易错命令
alias LS='ls'
alias sl='ls'
alias CD='cd'

alias vi='vim'
alias sudo='sudo '

# Plugins
alias fuck='eval $(thefuck $(fc -ln -1))'
alias poli='http_proxy=http://localhost:8123'


#docker
alias docker-stop-all='docker ps | awk "{print $1}" | xargs docker stop'
alias docker-rm-all='docker ps | awk "{print $1}" | xargs docker rm'

alias bfg='java -jar /usr/local/lib/bfg-1.12.15/bfg.jar'

